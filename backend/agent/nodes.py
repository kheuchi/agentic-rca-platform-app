"""LangGraph node functions for the RCA agent — Phase 4.4 implementation."""

# TODO Phase 4.4: Implement nodes:
# - plan_search(state) -> decide which tools to call
# - correlate_findings(state) -> LLM analyzes evidence, produces hypotheses
# - should_continue(state) -> bool (enough evidence? iteration < max?)
# - synthesize_root_cause(state) -> final RCA report
