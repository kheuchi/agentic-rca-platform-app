"""Docling parsing step — extract structured content from code files."""

# TODO Phase 4.2: Implement parse_files(repo_dir, file_patterns)
# - Docling for markdown, config files (YAML, Dockerfile)
# - Fallback to raw text for .py/.go/.java (LlamaIndex CodeSplitter handles these)
# - Return list of parsed documents with metadata
