"""Git clone step — shallow clone of target repo."""

# TODO Phase 4.2: Implement clone_repo(repo_url, branch, dest_dir)
# - Shallow clone (depth=1) for speed
# - Filter by services[] if provided
# - Return path to cloned repo
